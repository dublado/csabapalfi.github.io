{
  "name": "csabapalfi.github.io",
  "version": "1.0.0",
  "scripts": {
    "start": "github-markdown-html start",
    "build": "github-markdown-html build",
    "deploy": "npm run build && cp dist/index.html . && git commit -a -m 'gh-pages update' && git push"
  },
  "gh-pages-deploy": {
    "staticpath": ".",
    "prep": [
      "build"
    ]
  },
  "dependencies": {
    "github-markdown-html": "^2.5.1"
  }
}
